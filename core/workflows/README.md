# Claude Code Workflows

This directory contains Claude Code slash-command workflow files that work with both Next.js and Astro frameworks.

## Available Workflows

These commands can be used in Claude Code by referencing them in `.claude/commands/`.

## Adding Workflows

Add new workflow files as markdown documents with:
- Clear command name
- Description of what the command does
- Expected inputs and outputs
- Example usage